{
  "name": "@rainbowlaces/base-monorepo",
  "private": true,
  "scripts": {
    "deps:check": "syncpack list-mismatches",
    "deps:fix": "syncpack fix-mismatches",
    "release:base": "git stash -u && pnpm changeset && git add .changeset && git commit -m 'chore: Add changeset' && pnpm changeset version && git add . && git commit -m 'chore(release): Version packages' && pnpm --filter \"@rainbowlaces/base\" release && pnpm changeset publish && git stash pop"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.6",
    "@eslint/js": "^9.33.0",
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "eslint": "^9.33.0",
    "nodemon": "^3.1.10",
    "syncpack": "^13.0.4",
    "typescript": "^5.9.2"
  }
}