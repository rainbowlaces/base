{
  "name": "@rainbowlaces/base-monorepo",
  "private": true,
  "scripts": {
    "deps:check": "syncpack list-mismatches",
    "deps:fix": "syncpack fix-mismatches",
    "release:base": "pnpm changeset && git add .changeset && git commit -m 'chore: Add changeset' && pnpm changeset version && git add . && git commit -m 'chore(release): Version packages' && pnpm --filter \"@rainbowlaces/base\" release && pnpm changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@eslint/js": "^9.38.0",
    "@types/node": "^24.9.2",
    "esbuild": "^0.25.11",
    "eslint": "^9.38.0",
    "nodemon": "^3.1.10",
    "syncpack": "^13.0.4",
    "typescript": "^5.9.3"
  }
}